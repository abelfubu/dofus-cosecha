import { LazyImagesDirective } from './lazy-images.directive';

describe('LazyImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyImagesDirective();
    expect(directive).toBeTruthy();
  });
});
